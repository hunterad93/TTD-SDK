
from enum import Enum

class TargetingDataState(Enum):
    """
    None
    """
    ACTIVE = "ACTIVE"
    ARCHIVED = "ARCHIVED"
    AVAILABLE = "AVAILABLE"
    CREATED = "CREATED"
    EXPIRED = "EXPIRED"
